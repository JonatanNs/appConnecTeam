# Architecture Spring Boot

## 1 - Dépendances principales

| Dépendance | Utilité |
| --- | --- |
| Spring Web | Création API REST |
| Spring Security | Authentification et sécurisation API |
| Spring Data JPA | Communication avec la base de données |
| PostgreSQL Driver | Connexion PostgreSQL |
| Validation | Validation des données (`@Valid`) |
| Lombok | Réduction du boilerplate Java |
| MapStruct | Génération automatique des mappers entre entités et DTOs |
| WebSocket | Communication temps réel |
| SpringDoc OpenAPI | Documentation Swagger/OpenAPI |
| Java Mail Sender | Emails et notifications |

---

## 2 - Partie sécurité

| Dépendance | Utilité |
| --- | --- |
| Spring Security | JWT, rôles, permissions |
| JJWT (ou équivalent) | Génération et validation des tokens JWT |
| Validation | Validation sécurisée des entrées |

---

## 3 - Organisation du code : architecture par fonctionnalités (feature-based)

Le backend est organisé par fonctionnalité métier (*feature*) plutôt que par couche technique. Chaque module (ex: `Messaging`, `Users`, `Notifications`) regroupe ses propres entités, DTOs, mappers, repositories, services et controllers.

Cette organisation permet :

- une meilleure lisibilité et navigation dans le code ;
- un couplage plus faible entre les fonctionnalités ;
- une évolution plus simple du projet (ajout, modification ou suppression d'une fonctionnalité sans impacter les autres) ;
- une meilleure séparation des responsabilités, chaque module étant autonome.

```
└── java
    └── com
        └── nexteam
            ├── NexTeamApplication.java
            ├── advice
            ├── common
            ├── config
            ├── exceptions
            ├── security
            ├── websocket
            └── features
                ├── users
                ├── messanging
```

---

## 4 - Gestion du problème N+1

Le problème N+1 est un piège classique avec JPA/Hibernate : il survient lorsqu'une requête initiale charge une liste d'entités, puis qu'une requête supplémentaire est exécutée pour chaque entité afin de charger ses relations, entraînant un nombre de requêtes SQL proportionnel à la taille de la liste.

Afin d'éviter ce problème de performance, plusieurs bonnes pratiques sont appliquées :

- utilisation de `FetchType.LAZY` par défaut sur les relations, pour ne charger les données associées que lorsque c'est nécessaire ;
- utilisation de `@EntityGraph` sur les repositories afin de charger explicitement et en une seule requête les relations nécessaires selon le contexte d'utilisation ;
- configuration d'un batch fetching global via Hibernate, qui regroupe les accès aux relations restées en `LAZY` par lots plutôt que d'exécuter une requête par entité ;
- utilisation de DTOs projetés via MapStruct, plutôt que de charger des entités complètes avec toutes leurs relations.

Cette approche permet de garantir de bonnes performances, même lorsque le volume de données augmente.

### 🔹Exemple : `@EntityGraph`

```java
public interface UserRepository extends JpaRepository<User, Long> {

    @EntityGraph(attributePaths = {"roles", "address"})
    Optional<User> findByEmail(String email);

    @EntityGraph(attributePaths = {"roles", "address"})
    Optional<User> findByPublicId(UUID publicId);

    ...
}
```

`@EntityGraph` permet de préciser, requête par requête, quelles relations doivent être chargées en une seule fois via un `JOIN`, plutôt que de déclencher une requête supplémentaire à chaque accès à `user.getRoles()` ou `user.getAddress()`.

### 🔹Batch fetching

Pour les relations qui restent en `LAZY` sans `@EntityGraph` explicite, un batch fetching est configuré globalement afin de limiter l'impact du N+1 lorsqu'il n'est pas possible ou pertinent d'utiliser un `JOIN FETCH` :

Plutôt que d'exécuter une requête SQL par entité pour charger une relation manquante, Hibernate regroupe ces accès et les résout en une seule requête `IN (...)` par lot de 20 identifiants, réduisant considérablement le nombre total de requêtes exécutées.


```properties

# Permet à Hibernate de regrouper les accès aux relations LAZY en lots,
# via une requête WHERE id IN (...), par paquets de 20.
spring.jpa.properties.hibernate.default_batch_fetch_size=20

```

---

## 6 - Dépendances tests 

| Dépendance | Utilité |
| --- | --- |
| Spring Boot Starter Test | Tests unitaires |
| Mockito | Mock des dépendances |
| Testcontainers | Tests réalistes PostgreSQL |

---

## 7 - Documentation API

| Dépendance | Utilité |
| --- | --- |
| SpringDoc OpenAPI | Swagger UI automatique |

### 🔹URL Swagger

http://localhost:8080/swagger-ui/index.html